// frontend/src/utils/index.js
export const formatPrice = (price) => {
    return `$${parseFloat(price).toFixed(2)}`;
  };
  
  export const validateSKU = (sku) => {
    // იმპლემენტაცია SKU ვალიდაციისთვის
  };